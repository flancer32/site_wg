- Goal: rework the Russian homepage positioning for GitHub Flows App around a self-hosted GitHub-to-agent installation offer.
- Completed actions:
  - rewrote `tmpl/web/ru/index.html` to replace the broad ADSM-first homepage framing with a concrete `github-flows-app` installation and workflow automation offer;
  - reordered the page sections to match the requested structure: hero, installation scope, narrow pilot scope, limitations, example workflows, public demo, ADSM as method layer, credibility/background, final pilot CTA;
  - updated the Russian hero, demo framing, trust-building limitation copy, workflow examples, and CTA language to avoid unsupported claims and present Codex as one possible CLI agent.
- Validation:
  - `npm start` reached the documented SSR entrypoint but failed on port `3000` because it is already in use in the environment;
  - `TEQ_CMS_SERVER_PORT=3001 npm start` reached the same entrypoint but failed with `EPERM` when binding the port inside the sandbox;
  - no separate build or static validation command is documented in the repository README.
- Artifacts:
  - `tmpl/web/ru/index.html`

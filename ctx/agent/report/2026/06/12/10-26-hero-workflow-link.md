- Goal: упростить горячее предложение на русской домашней странице и заменить техническую цепочку более понятной ссылкой.
- Completed actions:
  - обновил `tmpl/web/ru/index.html`;
  - убрал строку `GitHub event → workflow rule → routing / agent step → comment / label / linked issue / PR / log` из hero;
  - заменил текст ссылки `Как это работает` на `Что такое исполняемый workflow` с той же ссылкой на страницу controlled product evolution;
  - удалил неиспользуемые стили `.flow-line`.
- Validation:
  - отдельная техническая валидация не запускалась, так как изменение было локальным в уже проверенном шаблоне.
- Artifacts:
  - `tmpl/web/ru/index.html`

- Goal: повторно проверить русскую домашнюю страницу после освобождения порта `3000`.
- Completed actions:
  - запустил локальный SSR-сервер командой `npm start`;
  - проверил HTTP-ответ страницы `http://127.0.0.1:3000/ru/`;
  - остановил локальный сервер после проверки.
- Validation:
  - сервер стартовал на порту `3000` без ошибки `EADDRINUSE`;
  - `curl -i http://127.0.0.1:3000/ru/` вернул `HTTP/1.1 200 OK`;
  - в ответе присутствует обновлённая структура русской домашней страницы: hero, блок "Кто я", три продукта, методический слой с книгой, финальный CTA.
- Artifacts:
  - runtime check only
